# function antimaining(){
#  PID=$(pidof kinsing)
#  echo "$PID"
#  kill -9 $PID

#  PID=$(pidof /tmp/kinsing)
#  echo "$PID"
#  kill -9 $PID

#  PID=$(pidof kdevtmpfsi)
#  echo "$PID"
#  kill -9 $PID

#  PID=$(pidof /tmp/kdevtmpfsi)
#  echo "$PID"
#  kill -9 $PID

#  PID=$(pidof /dev/shm/kdevtmpfsi)
#  echo "$PID"
#  kill -9 $PID

#  find / -iname kdevtmpfsi -exec rm -fv {} \;
#  find / -iname kinsing -exec rm -fv {} \;   
# }

# echo shell_exec(". /home/prod/.nf_shell/syncNaturalfarm.sh && sync_product"); 
